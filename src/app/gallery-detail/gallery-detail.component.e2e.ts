'use strict';

describe('Galleries Detail Page', () => {
    beforeEach(() => {
        //browser.get('/galleries');
    });

    it('should have h3 header', () => {
        //let subject = element(by.tagName('h3')).isPresent();
        //expect(subject).toEqual(true);
    });
});
